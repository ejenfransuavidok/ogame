<?= $this->json(
    array(
        'result'    => $this->result, 
        'auth'      => $this->auth, 
        'message'   => $this->message, 
        'event_id'  => $event_id, 
        'content'   => $planetkeep,
        'popup_building'     => $popup_building,
        'begin'     => $begin,
        'end'       => $end,
        'now'       => $now)) ?>
